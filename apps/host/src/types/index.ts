/**
 * Enum representing different app items.
 * @enum {number}
 */
export enum AppItem {
  /**
   * Represents the app item for "Rick and Morty".
   */
  RICK_AND_MORTY = 0,
  /**
   * Represents the app item for "Harry Potter".
   */
  HARRY_POTTER = 1,
}

/**
 * Enum representing different supported languages.
 * @enum {string}
 */
export enum LangItem {
  /**
   * Represents the lang item for "en".
   */
  EN = "en",
  /**
   * Represents the lnag item for "es".
   */
  ES = "es",
}
